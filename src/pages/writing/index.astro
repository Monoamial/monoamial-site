---
import Base from '../../layouts/Base.astro';
import EntryCard from '../../components/EntryCard.astro';
import { getCollection } from 'astro:content';

const entries = (await getCollection('writing', (e) => !e.data.draft))
  .sort((a, b) => +b.data.date - +a.data.date);
---
<Base title="Writing" description="Expository notes and essays on mathematics and physics.">
  <header class="page-head">
    <h1>Writing</h1>
    <p>Expository notes and essays on mathematics and physics.</p>
  </header>
  <div class="list">
    {entries.length === 0 && <p class="empty">No entries yet.</p>}
    {entries.map((e) => (
      <EntryCard
        href={`/writing/${e.id}/`}
        title={e.data.title}
        date={e.data.date}
        description={e.data.description}
        tags={e.data.tags}
      />
    ))}
  </div>
</Base>
<style>
  .page-head { margin: 1.5rem 0 2rem; }
  .page-head h1 { color: var(--accent-3); }
  .list { display: grid; gap: 1.1rem; }
  .empty { color: var(--chalk-faint); font-style: italic; }
</style>
